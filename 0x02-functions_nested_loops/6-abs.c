#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @y: the integer to be computed
 * Return: the absolute value
 */
int _abs(int y)
{
	if (y >= 0)
		return (y);
	else
		return (-y);
}
