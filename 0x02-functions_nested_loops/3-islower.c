#include "main.h"

/**
 * _islower - checks for lower case characters
 * @w: the character
 * Return: 1 if the letter is lowercase, 0 if not
 */

int _islower(int w)
{
	if (w >= 'a'&& w <= 'z')
		return (1);
	else
i		return (0);
}
